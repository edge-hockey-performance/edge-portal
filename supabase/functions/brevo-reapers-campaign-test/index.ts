import { createClient } from "npm:@supabase/supabase-js@2";

type Json = Record<string, unknown>;
const encoder = new TextEncoder();
const CAMPAIGN_NAME = "EDGE | Chicago Reapers Membership | Jordan Proof";
const TEST_RECIPIENT = "jordan@edgehockeyperformance.com";
const LIST_ID = 4;
const PRODUCT_1_URL = "https://edgehockeyperformance.com/products/edge-1-set-membership";
const PRODUCT_2_URL = "https://edgehockeyperformance.com/products/edge-2-set-membership";
const LOGO_URL = "https://edgehockeyperformance.com/cdn/shop/t/62/assets/edge-logo-nav.png?v=142111388735342124721786899168";

function json(status: number, body: Json) { return new Response(JSON.stringify(body), { status, headers: { "content-type": "application/json; charset=utf-8" } }); }
function safeEqual(a: string, b: string) { const left = encoder.encode(a); const right = encoder.encode(b); if (left.length !== right.length) return false; let mismatch = 0; for (let i = 0; i < left.length; i++) mismatch |= left[i] ^ right[i]; return mismatch === 0; }
function text(value: unknown) { return String(value ?? "").trim(); }
async function brevo(path: string, apiKey: string, init: RequestInit = {}) {
  const response = await fetch(`https://api.brevo.com/v3${path}`, { ...init, headers: { "api-key": apiKey, "accept": "application/json", "content-type": "application/json", ...(init.headers || {}) } });
  const raw = await response.text(); let body: Json = {};
  if (raw) { try { body = JSON.parse(raw) as Json; } catch { body = { raw: raw.slice(0, 500) }; } }
  if (!response.ok) throw new Error(`Brevo ${response.status} ${path}: ${JSON.stringify(body)}`);
  return { status: response.status, body };
}

const htmlContent = `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Chicago Reapers: choose your EDGE membership</title></head><body style="margin:0;padding:0;background:#0A0A0A;color:#FFFFFF;font-family:Arial,Helvetica,sans-serif;"><div style="display:none;max-height:0;overflow:hidden;opacity:0;color:transparent;">Compare the 1-Set and 2-Set options, then complete your membership securely through Shopify.</div><table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="background:#0A0A0A;"><tr><td align="center" style="padding:28px 14px;"><table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="max-width:640px;background:#141414;border:1px solid #2A2A2A;border-radius:14px;overflow:hidden;"><tr><td style="padding:30px 30px 16px;"><img src="${LOGO_URL}" width="190" alt="EDGE Performance" style="display:block;width:190px;max-width:100%;height:auto;margin:0 0 28px;"><div style="font-size:12px;line-height:1.4;letter-spacing:.14em;text-transform:uppercase;color:#B8D4E8;font-weight:800;">Chicago Reapers · Weekly blade management</div><h1 style="margin:10px 0 16px;font-size:36px;line-height:1.02;letter-spacing:-.02em;text-transform:uppercase;color:#FFFFFF;">Choose the EDGE membership that fits your player</h1><p style="margin:0 0 18px;color:#D7D7D7;font-size:16px;line-height:1.65;">Hi {{ contact.FNAME | default: "Reapers Parent" }},</p><p style="margin:0 0 20px;color:#D7D7D7;font-size:16px;line-height:1.65;">You asked for more information about EDGE Performance’s weekly blade-management program for Chicago Reapers players. Both options use the same documented EDGE process. The difference is the number of matching steel sets included in each weekly service cycle.</p></td></tr><tr><td style="padding:0 30px 16px;"><table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="background:#181818;border:1px solid #333333;border-radius:12px;"><tr><td style="padding:24px;"><div style="font-size:12px;letter-spacing:.12em;text-transform:uppercase;color:#A8AEBB;font-weight:800;">One matching pair</div><h2 style="margin:7px 0 8px;font-size:27px;line-height:1.08;text-transform:uppercase;color:#FFFFFF;">EDGE 1-Set Membership</h2><p style="margin:0 0 8px;color:#D7D7D7;font-size:15px;line-height:1.6;">For players managing one matching pair of removable runners.</p><p style="margin:0 0 20px;color:#FFFFFF;font-size:20px;line-height:1.4;font-weight:800;">$13 billed weekly <span style="color:#A8AEBB;font-weight:400;">or</span> $300 upfront</p><a href="${PRODUCT_1_URL}" style="display:inline-block;background:#B8D4E8;color:#0A0A0A;text-decoration:none;font-size:14px;font-weight:900;text-transform:uppercase;letter-spacing:.05em;padding:14px 18px;border-radius:6px;">View the 1-Set option</a></td></tr></table></td></tr><tr><td style="padding:0 30px 24px;"><table role="presentation" width="100%" cellspacing="0" cellpadding="0" style="background:#181818;border:1px solid #333333;border-radius:12px;"><tr><td style="padding:24px;"><div style="font-size:12px;letter-spacing:.12em;text-transform:uppercase;color:#A8AEBB;font-weight:800;">Up to two matching pairs</div><h2 style="margin:7px 0 8px;font-size:27px;line-height:1.08;text-transform:uppercase;color:#FFFFFF;">EDGE 2-Set Membership</h2><p style="margin:0 0 8px;color:#D7D7D7;font-size:15px;line-height:1.6;">For players who want up to two matching pairs included in each weekly service cycle.</p><p style="margin:0 0 20px;color:#FFFFFF;font-size:20px;line-height:1.4;font-weight:800;">$19 billed weekly <span style="color:#A8AEBB;font-weight:400;">or</span> $440 upfront</p><a href="${PRODUCT_2_URL}" style="display:inline-block;background:#B8D4E8;color:#0A0A0A;text-decoration:none;font-size:14px;font-weight:900;text-transform:uppercase;letter-spacing:.05em;padding:14px 18px;border-radius:6px;">View the 2-Set option</a></td></tr></table></td></tr><tr><td style="padding:0 30px 30px;"><div style="border-top:1px solid #2A2A2A;padding-top:22px;"><p style="margin:0 0 14px;color:#D7D7D7;font-size:15px;line-height:1.65;">After payment is confirmed, we’ll email a secure invitation to set up the EDGE Portal. The portal keeps the player’s blade preferences, service details, and history connected to the membership.</p><p style="margin:0;color:#D7D7D7;font-size:15px;line-height:1.65;">If you are unsure which option fits the player’s current steel setup, reply to this email and Jordan will help.</p><p style="margin:22px 0 0;color:#FFFFFF;font-size:15px;line-height:1.6;font-weight:700;">Jordan<br>EDGE Performance</p></div></td></tr><tr><td style="padding:20px 30px;background:#111111;border-top:1px solid #242424;text-align:center;"><p style="margin:0;color:#8E8E8E;font-size:12px;line-height:1.55;">You’re receiving this because you requested information about EDGE Performance for Chicago Reapers players.</p><p style="margin:8px 0 0;color:#8E8E8E;font-size:12px;line-height:1.55;"><a href="{{ unsubscribe }}" style="color:#B8D4E8;text-decoration:underline;">Unsubscribe</a></p></td></tr></table></td></tr></table></body></html>`;

Deno.serve(async (req: Request) => {
  if (req.method !== "POST") return json(405, { error: "method_not_allowed" });
  const supabaseUrl = Deno.env.get("SUPABASE_URL"); const serviceRoleKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY"); const brevoApiKey = Deno.env.get("BREVO_API_KEY");
  if (!supabaseUrl || !serviceRoleKey || !brevoApiKey) return json(503, { error: "campaign_test_not_configured" });
  const supabase = createClient(supabaseUrl, serviceRoleKey, { auth: { persistSession: false, autoRefreshToken: false } });
  const { data: storedSyncToken } = await supabase.rpc("get_secret", { secret_name: "brevo_sync_invocation_token" });
  const authorization = (req.headers.get("authorization") || "").trim(); const bearerToken = authorization.startsWith("Bearer ") ? authorization.slice(7).trim() : ""; const suppliedToken = (req.headers.get("x-edge-sync-token") || "").trim();
  const serviceRoleAuthorized = safeEqual(bearerToken, serviceRoleKey); const dedicatedTokenAuthorized = typeof storedSyncToken === "string" && storedSyncToken.length >= 32 && safeEqual(suppliedToken, storedSyncToken);
  if (!serviceRoleAuthorized && !dedicatedTokenAuthorized) return json(401, { error: "campaign_test_authorization_required" });
  const campaignPayload = { name: CAMPAIGN_NAME, subject: "Chicago Reapers: choose your EDGE membership", sender: { name: "EDGE Hockey Performance", email: "jordan@edgehockeyperformance.com" }, replyTo: "jordan@edgehockeyperformance.com", type: "classic", htmlContent, recipients: { listIds: [LIST_ID] }, previewText: "Compare the 1-Set and 2-Set options, then complete your membership securely through Shopify." };
  const draftsResponse = await brevo("/emailCampaigns?type=classic&status=draft&limit=50&offset=0&sort=desc", brevoApiKey);
  const campaigns = Array.isArray(draftsResponse.body.campaigns) ? draftsResponse.body.campaigns as Json[] : []; const existing = campaigns.find((campaign) => text(campaign.name) === CAMPAIGN_NAME);
  let campaignId = Number(existing?.id); let draftAction = "updated";
  if (Number.isInteger(campaignId) && campaignId > 0) await brevo(`/emailCampaigns/${campaignId}`, brevoApiKey, { method: "PUT", body: JSON.stringify(campaignPayload) });
  else { const created = await brevo("/emailCampaigns", brevoApiKey, { method: "POST", body: JSON.stringify(campaignPayload) }); campaignId = Number(created.body.id); draftAction = "created"; }
  if (!Number.isInteger(campaignId) || campaignId < 1) throw new Error("Brevo campaign ID was not returned");
  await brevo(`/emailCampaigns/${campaignId}/sendTest`, brevoApiKey, { method: "POST", body: JSON.stringify({ emailTo: [TEST_RECIPIENT] }) });
  return json(200, { status: "test_sent", campaign_id: campaignId, draft_action: draftAction, test_recipient: TEST_RECIPIENT, audience_list_id: LIST_ID, audience_send_executed: false });
});
